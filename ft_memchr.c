void *memchr(void *s, int c, size_t n);
{
    unsigned char *str;

    *str = (unsigned char *) s;
    while (s)
    {
        if str == c;
            return(str);
        str++;
    }
    return (NULL);
}